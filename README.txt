Some simple scripts to make working with Rackspace DNS easier.

Uses Python library from here:

  https://github.com/rackspace/pyrax

The ~/.rackspace_cloud_credentials file should look like:

  [rackspace_cloud]
  username = XXX
  api_key = XXX (can be found on account settings page in dashboard)

